node_modules/
.DS_Store
*.log
.env
.idea/
.vscode/
dist/
build/
*.swp
*.swo
*~

# Firebase Hosting build output — regenerate with `npm run build-deploy`.
# Everything in it is derived, so it must not be committed.
firebase-deploy/

# Downloaded data — multi-GB and regenerable (see README).
data/static-gtfs/
data_cache/

# YOLO detection model for the visual-quality counter — 4 MB of weights,
# reproducible with `node tools/fetch-yolo-model.mjs`, so it is fetched rather
# than committed, for the same reason as the data above.
models/*.onnx

# Local UI-tuning screenshots
_*.png

